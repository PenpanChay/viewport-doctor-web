# viewport-doctor-web needs no environment variables or secrets to run
# locally. The scan runs in a Next.js API route on the server, launches a
# local headless browser, and visits the URLs you give it through the UI.
#
# Kept as a placeholder so the repository matches the standard project layout.
